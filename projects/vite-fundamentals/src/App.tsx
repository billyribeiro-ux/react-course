// Recipe Finder — you build this app up across Part 30, lesson by lesson.
// It starts as a friendly placeholder so the project runs from minute one.

export default function App() {
  return (
    <main className="app">
      <h1>🍳 Recipe Finder</h1>
      <p>
        Welcome! You&apos;ll grow this into a real recipe search app as you work
        through React Fundamentals — components, props, state, lists, and forms.
      </p>
    </main>
  );
}
