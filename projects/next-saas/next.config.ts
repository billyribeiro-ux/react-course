import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Next.js 16 enables the React Compiler with a single flag (it installs the
  // compiler under the hood). Clean code, auto-optimized. (Part 50.)
  reactCompiler: true,
};

export default nextConfig;
